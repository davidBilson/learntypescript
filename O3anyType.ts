// ANY TYPE
//  "any type" allows a variable to have any type of value, it will disable all type checking for that variable and its properties
let color: any = "dodgerblue";
color = 20;
color = true;
console.log(color);